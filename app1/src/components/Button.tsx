export const Button = ({ children }) => (
  <div style={{
    padding: "20px 30px",
    display: "grid",
    justifyItems: "center",
    alignItems: "center",
    background: "deepskyblue"
  }}>{children}</div>
)
